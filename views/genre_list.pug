extends layout

block content
    h1= title

    ul
        for genre in genre_list
            li
                a(href=genre.url) #{genre.name}

        else
            There are no genres in this library
